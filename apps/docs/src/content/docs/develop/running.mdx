---
title: Running Lys locally
description: Dev commands, watch modes, local endpoints, and current limitations.
sidebar:
  order: 2
---

import DocumentMeta from "@/components/handbook/DocumentMeta.astro"
import RelatedDocuments from "@/components/handbook/RelatedDocuments.astro"

Dev commands, watch modes, local endpoints, and current limitations.

<DocumentMeta
  label="Page metadata"
  entries={[{ label: "Type", value: "Guide" }]}
/>

## Root commands

Choose the command you need and run it from the repository root:

```sh
pnpm docs:dev
pnpm backend:dev
pnpm dev
```

## What each command does

- `pnpm docs:dev`: starts the Astro/Starlight docs app.
- `pnpm backend:dev`: runs `tsx watch src/index.ts` in `apps/backend`.
- `pnpm dev`: runs `pnpm --filter @lys/desktop run tauri dev`, which starts the
  desktop Vite app and the Tauri shell.

For standalone backend development, create the database parent directory before
starting the watcher:

```sh
mkdir -p "$HOME/.lys"
pnpm backend:dev
```

The backend opens `~/.lys/lys_db.sqlite` but does not create its parent. The
desktop-managed path creates the directory while initializing missing settings.
Both paths use the same database and port; choose one backend launch path.

## Runtime behavior to expect

- The desktop app talks to the backend over HTTP at `http://127.0.0.1:12345`.
- The chat transport is a `POST /api/v1/chat` request that returns an SSE
  stream.
- The backend uses LM Studio for model inventory and load operations and an
  OpenAI-compatible endpoint for chat completions.
- Backend initialization checks LM Studio readiness before listening for HTTP
  requests. Start LM Studio at `127.0.0.1:1234` before starting the backend.

## Backend lifecycle in the desktop app

The desktop starts and stops an owned `pnpm run dev` child through Tauri
commands. The command runs from the backend directory in the checkout recorded
when the host was compiled, so that checkout, installed dependencies, Node, and
`pnpm` must remain available.

`runtime.autoStartBackend` defaults to `true`; initialization starts the child
when the loaded value is enabled. Runtime control edits remain session-only;
generation controls have separate autosave wiring.

Runtime status describes the owned child process. It does not discover a
backend started separately in a terminal or prove HTTP readiness. Use
`GET /api/v1/heath` to check the listener. Avoid launching a standalone watcher
alongside desktop auto-start because both attempt to use port `12345`.

## Coming soon

The current backend configuration writes its SQLite database to
`~/.lys/lys_db.sqlite`. There is no contributor-facing command or documented
flag yet for swapping in a scratch data directory from the docs workflow.

<RelatedDocuments
  documents={[
    {
      kind: "Section",
      label: "Develop overview",
      href: "/develop/setup/"
    },
    {
      kind: "Decisions",
      label: "Related decision records",
      href: "/decisions/"
    },
    {
      kind: "Reference",
      label: "SSE events",
      href: "/reference/sse-events/"
    },
    {
      kind: "Reference",
      label: "API reference",
      href: "/reference/api/"
    }
  ]}
/>
