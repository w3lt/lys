---
title: Troubleshooting
description: Common symptoms mapped to causes and runbooks.
sidebar:
  order: 6
---

import DocumentMeta from "@/components/handbook/DocumentMeta.astro"
import RelatedDocuments from "@/components/handbook/RelatedDocuments.astro"

Current troubleshooting guidance grounded in the checked-in codebase.

<DocumentMeta
  label="Page metadata"
  entries={[{ label: "Type", value: "Guide" }]}
/>

## The desktop shows the backend as stopped

The desktop runtime pane controls the backend child owned by Tauri. It does not
discover a backend launched separately in a terminal. Choose one launch path
before starting another process on the same port.

For the desktop-managed path:

- confirm the runtime pane reports `stopped` rather than `starting`
- start the backend from that pane
- the pane switches to `running` when the owned child is reported running;
  uptime and the displayed address do not prove HTTP readiness
- verify `GET /api/v1/heath` responds at `http://127.0.0.1:12345`

If the listener is unavailable, inspect the backend output. Startup requires
LM Studio at `127.0.0.1:1234`; its readiness query completes before the backend
listens. Start LM Studio and restart the backend after a readiness failure.
For standalone startup, also ensure `~/.lys/` exists as described in
[Running Lys locally](/develop/running/).

## The backend starts but model operations fail

The backend is wired to LM Studio at `127.0.0.1:1234`. If model listing,
loading, or chat requests fail after HTTP readiness was established, verify
that LM Studio is still reachable. The
[model health endpoint](/operate/logging/#model-health-observation) can report
loaded state; it does not test inference success.

## Lys forgets runtime settings

The desktop host owns `~/.lys/settings.json`, but the checked-in desktop UI
currently loads settings without calling the Tauri save command after runtime
setting edits. If runtime preferences do not survive a restart:

- check whether `~/.lys/settings.json` exists
- if it exists, verify that it contains valid JSON
- if it does not exist, the host should recreate it the next time settings are
  loaded
- if the file exists but does not reflect a recent UI change, that matches the
  current implementation gap: save wiring exists in Tauri, but not in the UI

This limitation applies to runtime and model controls. Generation controls
autosave separately and expose a save failure with a retry action; a generation
save preserves the runtime and model values already on disk.

## Startup fails after a schema change

The conversation service reads SQLite `PRAGMA user_version` on startup and
refuses to open a database newer than the running binary supports. If a startup
failure mentions an unsupported database version, use a matching or newer build
rather than editing the database by hand.

## Known gaps

The repository does not currently include evidenced support for:

- automated store repair commands
- diagnostics bundles
- packaged log collection and rotation
- a published operator escalation path

Those workflows should be documented here only after the product ships them.

<RelatedDocuments
  documents={[
    {
      kind: "Section",
      label: "Logging and diagnostics",
      href: "/operate/logging/"
    },
    {
      kind: "Reference",
      label: "Data locations and lifecycle",
      href: "/operate/data/"
    },
    {
      kind: "Runbook",
      label: "Session recovery",
      href: "/operate/runbooks/recover-session-database/"
    }
  ]}
/>
