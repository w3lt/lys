---
title: Database schemas
description: The current SQLite tables, constraints, indexes, triggers, and settings JSON shape used by Lys.
docType: reference
sidebar:
  order: 4
---

The backend currently manages one SQLite schema and the Tauri host manages one
JSON schema for runtime, model, and generation settings.

## SQLite tables

### `conversations`

| Column          | Type            | Notes                                                                                              |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------- |
| `id`            | `TEXT`          | Primary key                                                                                        |
| `title`         | `TEXT`          | Null until title generation saves a title; a chat turn on an untitled conversation requests one    |
| `system_prompt` | `TEXT NOT NULL` | Prompt associated with the conversation; the current chat request does not forward it to the model |
| `created_at`    | `TEXT NOT NULL` | ISO timestamp                                                                                      |
| `updated_at`    | `TEXT NOT NULL` | ISO timestamp                                                                                      |

### `conversation_messages`

| Column            | Type            | Notes                                                                                                             |
| ----------------- | --------------- | ----------------------------------------------------------------------------------------------------------------- |
| `id`              | `TEXT`          | Primary key                                                                                                       |
| `conversation_id` | `TEXT NOT NULL` | Foreign key to `conversations(id)` with `ON DELETE CASCADE`                                                       |
| `role`            | `TEXT NOT NULL` | `user` or `assistant`                                                                                             |
| `model`           | `TEXT`          | Required for assistant rows, absent for user rows                                                                 |
| `content`         | `TEXT NOT NULL` | User content is non-empty; assistant content may remain empty because streamed deltas are not currently persisted |
| `status`          | `TEXT`          | Assistant-only: `streaming`, `completed`, `interrupted`, `failed`                                                 |
| `finish_reason`   | `TEXT`          | Assistant-only: `stop` or `length`, nullable while not completed                                                  |
| `created_at`      | `TEXT NOT NULL` | ISO timestamp                                                                                                     |
| `updated_at`      | `TEXT`          | Required for assistant rows                                                                                       |

## Important constraints

- User rows must have non-empty content and no model, status, or finish reason.
- Assistant rows must have a model and status.
- Completed assistant rows must also have a non-null finish reason.
- Streaming, interrupted, and failed assistant rows must keep a null finish
  reason.

The current backend inserts an empty assistant row before generation and later
updates its status and finish reason. It does not write streamed delta content
back to that row.

## Indexes and triggers

| Name                                       | Kind    | Purpose                                                  |
| ------------------------------------------ | ------- | -------------------------------------------------------- |
| `conversation_messages_conversation_idx`   | index   | Orders messages by conversation, creation time, and id   |
| `update_conversation_after_message_insert` | trigger | Updates `conversations.updated_at` after inserts         |
| `update_conversation_updated_at`           | trigger | Keeps `updated_at` monotonic on conversation updates     |
| `update_conversation_after_message_update` | trigger | Updates `conversations.updated_at` after message updates |

## Migration strategy

The backend applies schema changes from an ordered array of SQL strings and
tracks the applied version with `PRAGMA user_version`.

## Settings JSON

The Tauri host stores `runtime`, `model`, and `generation` groups in
`~/.lys/settings.json`. See the [configuration reference](/reference/configuration/#persisted-settings-json)
for the current fields, defaults, and validation. Generation edits are saved
automatically; runtime and model edits in the desktop remain session-only.

## Coming soon

- Public schema contracts for conversation listing and resume routes
- Export and import formats for persisted conversations
- Persisted conversation settings beyond the current runtime, model, and generation groups

## Related pages

- [Persistence and ownership](/architecture/persistence/)
- [Configuration reference](/reference/configuration/)
