---
title: End-to-end flows
description: "The concrete runtime flows that exist today: backend startup, chat streaming, and user-initiated stop."
docType: architecture
sidebar:
  order: 9
---

This page documents only the flows implemented in the current codebase.

## Start the backend

1. The React app calls `initialize`.
2. The desktop loads host-managed settings through `load_settings`.
3. If `autoStartBackend` is enabled, the desktop calls `start_backend`.
4. The Tauri host spawns `pnpm run dev` in `apps/backend`.
5. The desktop queries `get_backend_status`, records `running` or `stopped`,
   and completes initialization.
6. The shell store requests model inventory if the process is running.

For a stopped backend, a manual start sets the shell status to `starting` before invoking
`start_backend`, then records `running` when the host reports a live child.
This process observation does not establish that the backend HTTP API is ready.

## Send one chat message

1. The composer asks the chat store to `sendMessage`.
2. The desktop creates a request token and an `AbortController`.
3. The desktop sends `POST /api/v1/chat` with `message`, `model`, required
   `generationOptions`, and optional `conversationId`. Generation options contain
   the current temperature and an optional reply ceiling; a local zero ceiling
   is omitted from the request.
4. The backend creates or reuses the conversation, inserts the user message, and
   inserts an empty streaming assistant message.
5. The backend emits a `start-new-conversation-turn` or
   `start-existing-conversation-turn` event.
6. The backend streams zero or more `delta` events, then a `done` event when the
   model finishes successfully.
7. For a new conversation only, a parallel title task requests a title after
   the turn-start event. A usable title is persisted and emitted as a `title`
   event before, during, or after the assistant deltas and completion;
   otherwise the conversation keeps its default `New Conversation` title and no
   event is sent.
8. The desktop applies each event to immutable local conversation state.

## Stop an in-flight reply

1. The user presses Stop in the composer.
2. The desktop marks the local assistant message as `interrupted`.
3. The desktop aborts the owned request controller.
4. The backend SSE close handler aborts the upstream LM Studio request.
5. The stream ends without further UI updates for that request token.

## Error path

If opening the stream, reading SSE frames, validating payloads, or finishing the
model response fails, the desktop records a chat lifecycle error and may mark
the assistant message as `failed`.

## Coming soon

- Resume persisted conversations after relaunch
- Replay prior events into a new desktop window
- Credential rotation or secret refresh flows
- Guided crash recovery and database repair flows

## Related pages

- [Backend](/architecture/backend/)
- [SSE events](/reference/sse-events/)
- [Configuration reference](/reference/configuration/)
