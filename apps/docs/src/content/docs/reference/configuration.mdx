---
title: Configuration reference
description: The current constants, file paths, and settings used by the desktop and backend.
docType: reference
sidebar:
  order: 3
---

Lys currently uses shared loopback constants, backend runtime composition, a
Tauri-managed settings file, and one WebView theme preference.

## Shared constants

| Constant        | Value       | Used by                                      |
| --------------- | ----------- | -------------------------------------------- |
| `API_PREFIX_V1` | `/api/v1`   | Route path composition                       |
| `BACKEND_HOST`  | `127.0.0.1` | Backend bind host and desktop backend URL    |
| `BACKEND_PORT`  | `12345`     | Backend bind port and desktop backend URL    |
| `LMSTUDIO_HOST` | `127.0.0.1` | Backend LM Studio HTTP and WebSocket clients |
| `LMSTUDIO_PORT` | `1234`      | Backend LM Studio HTTP and WebSocket clients |

## Backend-composed values

| Value                        | Current source                                |
| ---------------------------- | --------------------------------------------- |
| Backend listen address       | `BACKEND_HOST` and `BACKEND_PORT`             |
| LM Studio HTTP base URL      | `http://${LMSTUDIO_HOST}:${LMSTUDIO_PORT}/v1` |
| LM Studio WebSocket base URL | `ws://${LMSTUDIO_HOST}:${LMSTUDIO_PORT}`      |
| SQLite path                  | `~/.lys/lys_db.sqlite`                        |

## Persisted settings JSON

The Tauri host reads and writes `~/.lys/settings.json`. A new file uses these
defaults:

```json
{
  "runtime": {
    "autoStartBackend": true,
    "defaultModel": null,
    "backendAddress": "http://127.0.0.1:12345"
  },
  "model": {
    "contextSize": 32768
  },
  "generation": {
    "temperature": 0.7,
    "replyCeiling": 2048
  }
}
```

| Key                        | Type                    | Meaning                                                                                     |
| -------------------------- | ----------------------- | ------------------------------------------------------------------------------------------- |
| `runtime.autoStartBackend` | `boolean`               | Whether desktop initialization should ask Tauri to start the backend automatically          |
| `runtime.defaultModel`     | `string \| null`        | Model selected for the settings load controls; chat currently uses its own fixed identifier |
| `runtime.backendAddress`   | `string`                | Backend address displayed in settings; HTTP clients still target the fixed loopback address |
| `model.contextSize`        | unsigned 32-bit integer | Context-size estimate displayed in the Model pane; not forwarded by the load API            |
| `generation.temperature`   | number in `[0, 1]`      | Sampling temperature sent with each new chat request                                        |
| `generation.replyCeiling`  | unsigned 32-bit integer | Maximum reply tokens; the desktop omits the request limit when this is `0`                  |

Missing groups or fields use the host defaults. Invalid JSON or invalid field
values, including a temperature outside `[0, 1]`, cause loading to fail. Loading
a missing file creates the parent directory and writes the default document.

## Settings updates

The settings view provides Runtime, Model, and Generation panes. Model inventory,
load, unload, and loaded-state health controls call the backend through the
application store. There is no Conversation pane.

Generation edits apply to future messages immediately and are saved
automatically through Tauri. The save reloads the persisted document and replaces
only its `generation` group, preserving runtime and model values already on disk.
Saves are serialized and newer edits are saved after the active write settles.
Save failure leaves the current values usable and exposes a retry action.

Runtime edits, including auto-start and default-model selection, remain in memory
for the current session. Model settings edits also remain session-only. A
generation save does not persist those unsaved groups.

## Desktop theme preference

The active desktop theme is stored separately in WebView `localStorage` under
`lys.theme`. Valid values are `dark` and `light`; the fallback is `dark` when
the value is missing, invalid, or unavailable.

## Current caveats

- The React shell loads runtime settings from Tauri, but edits in the Runtime
  tab are not persisted back through `save_settings`.
- The chat path currently uses a fixed model identifier in the chat store rather
  than `runtime.defaultModel`.
- Model context size is controlled by LM Studio at load time; the current Lys
  load request carries only the model identifier.

## Coming soon

- Persisting runtime and model edits back to `settings.json`
- Wiring selected model into live chat requests
- Configuring model load-time context size through the backend
- Release-mode configuration beyond the current loopback defaults

## Related pages

- [Backend API contracts](/reference/api/)
- [Database schemas](/reference/schema/)
- [Tauri host and lifecycle](/architecture/host/)
