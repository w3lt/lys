---
title: SSE events
description: The six concrete SSE payload variants currently emitted by the chat route.
docType: reference
sidebar:
  order: 2
---

The current chat route emits exactly six SSE payload variants. The payload
shape is defined by `chatApiStreamEventSchema` in `@lys/protocol`.

## Event variants

| Event name                         | Payload fields                                    | Meaning                                                              |
| ---------------------------------- | ------------------------------------------------- | -------------------------------------------------------------------- |
| `start-new-conversation-turn`      | `conversation`, `userMessage`, `assistantMessage` | The backend created a new conversation and persisted the first turn. |
| `start-existing-conversation-turn` | `userMessage`, `assistantMessage`                 | The backend persisted a new turn on an existing conversation.        |
| `title`                            | `title`                                           | A generated title for the active conversation.                       |
| `delta`                            | `content`                                         | One non-empty assistant text fragment.                               |
| `done`                             | `finishReason`                                    | Successful assistant completion with `stop` or `length`.             |
| `error`                            | `message`                                         | A user-presentable error string.                                     |

## Payload details

### `start-new-conversation-turn`

Includes:

- full `conversation` metadata
- one persisted user message
- one persisted assistant message in `streaming` state

### `start-existing-conversation-turn`

Includes:

- one persisted user message
- one persisted assistant message in `streaming` state

The desktop uses its current active conversation metadata together with these
messages to build the new transcript state.

### `title`

Carries a non-empty string title generated in parallel with the chat stream.

### `delta`

Carries one non-empty `content` fragment. The desktop appends it to the current
streaming assistant message.

### `done`

Carries:

```json
{
  "type": "done",
  "finishReason": "stop"
}
```

The only accepted `finishReason` values are `stop` and `length`.

### `error`

Carries a non-empty `message` string. The desktop presents it inline and may
also mark the current assistant message as failed locally.

## What is not on the current stream

- No `event_id`
- No `session_id`
- No `emitted_at`
- No replay marker such as `stream.resumed`
- No tool-call frame events

## Event ordering today

Every valid stream begins with one turn-start event. After that, chat generation
and title generation run concurrently:

1. `start-new-conversation-turn` or `start-existing-conversation-turn`
2. zero or more `delta` events before a successful `done`
3. a `title` event may arrive before, during, or after the chat events

An `error` event can report failure in either concurrent task. A title error does
not prevent later chat events, so `error` is not universally terminal.

## Coming soon

- Replay and resume markers if conversation restoration is added
- Tool-call or structured action stream events
- Stronger protocol versioning around additive event growth

## Related pages

- [Backend](/architecture/backend/)
- [Shared protocol](/architecture/protocol/)
