---
title: SSE events
description: The six concrete SSE payload variants currently emitted by the chat route.
docType: reference
sidebar:
  order: 2
---

The current chat route emits exactly six SSE payload variants. The payload
shape is defined by `chatApiStreamEventSchema` in `@lys/protocol`.

## Event variants

| Event name                         | Payload fields                                    | Meaning                                                              |
| ---------------------------------- | ------------------------------------------------- | -------------------------------------------------------------------- |
| `start-new-conversation-turn`      | `conversation`, `userMessage`, `assistantMessage` | The backend created a new conversation and persisted the first turn. |
| `start-existing-conversation-turn` | `userMessage`, `assistantMessage`                 | The backend persisted a new turn on an existing conversation.        |
| `title`                            | `title`                                           | A generated title for a newly created conversation.                  |
| `delta`                            | `content`                                         | One non-empty assistant text fragment.                               |
| `done`                             | `finishReason`                                    | Successful assistant completion with `stop` or `length`.             |
| `error`                            | `message`                                         | A user-presentable chat generation error string.                     |

## Payload details

### `start-new-conversation-turn`

Includes:

- full `conversation` metadata, whose `title` is the default `New Conversation`
- one persisted user message
- one persisted assistant message in `streaming` state

### `start-existing-conversation-turn`

Includes:

- one persisted user message
- one persisted assistant message in `streaming` state

The desktop uses its current active conversation metadata together with these
messages to build the new transcript state.

### `title`

Carries the non-empty title generated for a newly created conversation. The
backend persists the title before it sends the event. Continuing an existing
conversation never produces a `title` event.

The backend requests a title up to three times, set by the
`titleGenerationMaxAttempts` backend setting. Only a reply that cannot be used as
a title, such as JSON wrapped in a Markdown code fence, leads to another request;
an HTTP or connection failure ends title generation at once. When no usable
title is produced, or the client disconnects first, no `title` event is sent and
the conversation keeps `New Conversation`. The backend logs these outcomes
instead of sending an `error` event.

### `delta`

Carries one non-empty `content` fragment. The desktop appends it to the current
streaming assistant message.

### `done`

Carries:

```json
{
  "type": "done",
  "finishReason": "stop"
}
```

The only accepted `finishReason` values are `stop` and `length`.

### `error`

Carries a non-empty `message` string that reports a chat generation failure.
The desktop presents it inline and may also mark the current assistant message
as failed locally. Title-generation failures never produce `error` events.

## What is not on the current stream

- No `event_id`
- No `session_id`
- No `emitted_at`
- No replay marker such as `stream.resumed`
- No tool-call frame events

## Event ordering today

Every valid stream begins with one turn-start event. After that, chat
generation and, for a new conversation, title generation run concurrently:

1. `start-new-conversation-turn` or `start-existing-conversation-turn`
2. zero or more `delta` events before a successful `done`
3. for a new conversation, a `title` event may arrive before, during, or after
   the chat events

An `error` event reports a chat generation failure and is not followed by
`done`. A `title` event can still arrive after it, because title generation
runs independently.

## Coming soon

- Replay and resume markers if conversation restoration is added
- Tool-call or structured action stream events
- Stronger protocol versioning around additive event growth

## Related pages

- [Backend](/architecture/backend/)
- [Shared protocol](/architecture/protocol/)
