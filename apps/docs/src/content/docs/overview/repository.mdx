---
title: Repository map
description: The applications and shared packages in the pnpm workspace and what each currently owns.
sidebar:
  order: 4
---

import DocumentMeta from "@/components/handbook/DocumentMeta.astro"
import RelatedDocuments from "@/components/handbook/RelatedDocuments.astro"

The applications and shared packages in the pnpm workspace and what each
currently owns.

<DocumentMeta
  label="Page metadata"
  entries={[{ label: "Type", value: "Overview" }]}
/>

The workspace structure is simple and explicit:

## Root workspace

- `package.json` owns the root developer commands such as `pnpm dev`,
  `pnpm docs:dev`, and `pnpm backend:dev`.
- `pnpm-workspace.yaml` includes `apps/*` and `packages/*`.

## Applications

### `apps/desktop`

The local desktop UI. It uses React, Vite, Zustand, and Tauri. It starts and
stops the backend through Tauri commands, opens the chat SSE stream, and
renders conversation state for the user.

### `apps/backend`

The local service process. It registers Fastify routes for health checks, model
inventory and load operations, and the chat stream. It also owns the SQLite
conversation store and the LM Studio / OpenAI-compatible service adapters.

### `apps/docs`

The internal engineering handbook. It is built with Astro and Starlight and
validates handbook frontmatter at build time.

## Shared packages

### `packages/protocol`

The transport contract shared between desktop and backend. It exports route
constants, request schemas, and stream event schemas.

### `packages/share`

The shared domain model package. Today it mainly defines the conversation and
message schemas reused by both transport and persistence code.

## Coming soon

This repository does not currently publish an explicit reviewer-ownership map
or code-owner index inside the handbook. Until that exists, use the package
layout, commit history, and the relevant standards documents to find the closest
maintained context.

<RelatedDocuments
  documents={[
    {
      kind: "Section",
      label: "Introduction",
      href: "/overview/introduction/"
    },
    {
      kind: "Decisions",
      label: "Related decision records",
      href: "/decisions/"
    },
    {
      kind: "Guide",
      label: "Environment setup",
      href: "/develop/setup/"
    }
  ]}
/>
